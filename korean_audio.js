/* KoreanLang Audio — placeholder. Falls back to the browser's built-in
   Korean TTS voice (see speakKorean() in app.js). Populate with base64
   pre-recorded clips later if native-speaker audio is desired for v2. */
var KOREAN_AUDIO_F = {};
var KOREAN_AUDIO_M = {};
